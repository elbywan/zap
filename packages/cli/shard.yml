name: cli
version: 0.1.0
crystalline:
  main: zap.cr
  flags:
    - preview_mt
    - execution_context
targets:
  zap:
    main: zap.cr
dependencies:
  core:
    path: ../core
  utils:
    path: ../utils
  commands:
    path: ../commands
